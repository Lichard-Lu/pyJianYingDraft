# pyJianYingDraft 在 macOS 上的使用指南

## 🎯 核心问题分析

### ✅ **好消息：项目在 macOS 上可以使用！**

根据检查结果：

1. **项目兼容性**：`pyJianYingDraft` 支持 macOS 系统
2. **功能支持**：在 macOS 上支持草稿生成和模板模式
3. **实际测试**：你的 `demo.py` 已经成功创建了草稿文件

### ⚠️ **问题原因：剪映版本兼容性**

**根本问题**：你的剪映版本可能是 6.0+，导致草稿文件被加密，与 pyJianYingDraft 生成的未加密 JSON 格式不兼容。

## 📊 当前状态分析

### 你的剪映草稿状态：

```
现有草稿分析：
├── 8月25日 (1)      - draft_info.json: 3.3MB (已加密)
├── 8月25日          - draft_info.json: 2.7MB (已加密)
└── demo (新建)      - draft_content.json: 20KB (未加密JSON)
```

**关键发现**：
- 旧草稿：加密的 `draft_info.json` 文件（剪映 6.0+ 格式）
- 新草稿：未加密的 `draft_content.json` 文件（pyJianYingDraft 生成格式）

### pyJianYingDraft 支持的功能：

| 功能 | Windows | macOS | Linux |
|------|---------|-------|-------|
| 草稿生成 | ✅ | ✅ | ✅ |
| 模板模式 | ✅ | ✅ | ✅ |
| 自动导出 | ✅ | ❌ | ❌ |

## 🛠️ 解决方案

### 方案 1：降级剪映版本（推荐）

**目标**：安装剪映 5.9 版本

```bash
# 1. 备份当前草稿
cp -r ~/Movies/JianyingPro ~/Movies/JianyingPro_Backup

# 2. 卸载当前剪映版本
# 删除 /Applications 中的剪映应用

# 3. 下载剪映 5.9 版本
# 需要找到历史版本下载链接
```

**剪映 5.9 下载资源**：
- 官方通常不提供历史版本下载
- 可以尝试第三方软件下载网站
- 或者寻找网友分享的安装包

### 方案 2：使用虚拟机

```bash
# 在虚拟机中安装：
# - Windows 10/11 + 剪映最新版
# - macOS + 剪映 5.9

# 在虚拟机中运行 pyJianYingDraft 生成草稿
# 然后将草稿文件复制到主系统
```

### 方案 3：混合工作流

**当前可行的做法**：

1. **使用 pyJianYingDraft 生成草稿** ✅
   ```python
   import pyJianYingDraft as draft

   draft_folder = draft.DraftFolder("/Users/xjc/Movies/JianyingPro/User Data/Projects/com.lveditor.draft")
   script = draft_folder.create_draft("my_project", 1920, 1080)
   # ... 添加内容
   script.save()
   ```

2. **手动转换草稿格式** ⚠️
   - 在剪映 6.0+ 中创建新草稿
   - 复制 pyJianYingDraft 生成的内容
   - 手动重新添加素材和效果

### 方案 4：等待项目更新

- 关注项目 GitHub 仓库更新
- 支持剪映 6.0+ 解密功能
- 参与项目贡献

## 🚀 立即可用的测试

### 验证 pyJianYingDraft 工作状态：

```bash
# 1. 激活虚拟环境
source venv/bin/activate

# 2. 运行配置好的测试
python demo_configured.py

# 3. 检查生成的草稿
ls ~/Movies/JianyingPro/User\ Data/Projects/com.lveditor.draft/demo_test/
```

### 验证草稿文件格式：

```bash
# 检查是否为有效 JSON
python -c "
import json
with open('/Users/xjc/Movies/JianyingPro/User Data/Projects/com.lveditor.draft/demo/draft_content.json') as f:
    data = json.load(f)
    print('✅ JSON 格式有效')
    print(f'分辨率: {data[\"canvas_config\"][\"width\"]}x{data[\"canvas_config\"][\"height\"]}')
"
```

## 💡 实用建议

### 1. 当前限制

- ❌ 无法在剪映 6.0+ 中直接打开 pyJianYingDraft 生成的草稿
- ❌ 无法使用自动导出功能
- ✅ 可以生成草稿文件
- ✅ 可以作为模板在其他剪映版本中使用

### 2. 工作流程建议

```python
# 创建草稿
import pyJianYingDraft as draft

# 1. 生成草稿
draft_folder = draft.DraftFolder("/path/to/draft/folder")
script = draft_folder.create_draft("project_name", 1920, 1080)

# 2. 添加内容（轨道、片段等）
script.add_track(draft.TrackType.video)
# ... 添加更多内容

# 3. 保存为标准 JSON 格式
script.save()

# 4. 导出项目信息供参考
print(f"草稿时长: {script.duration / 1000000:.2f} 秒")
print(f"轨道数量: {len(script.tracks)}")
```

### 3. 替代方案

如果需要完整功能，考虑：

1. **剪映 5.9 + Windows 虚拟机**
   - 最完整的兼容性
   - 支持所有功能

2. **其他视频编辑软件**
   - DaVinci Resolve（免费版功能强大）
   - Final Cut Pro（Mac 专用）
   - Adobe Premiere Pro

## 🔧 故障排除

### 如果草稿无法打开：

1. **检查剪映版本**
   ```bash
   # 查看剪映应用信息
   mdls /Applications/JianyingPro.app
   ```

2. **检查草稿文件**
   ```bash
   # 验证 JSON 格式
   python -m json.tool path/to/draft_content.json
   ```

3. **查看剪映日志**
   ```bash
   # 剪映日志通常在：
   ~/Library/Logs/JianyingPro/
   ```

## 📚 参考资料

- [项目 GitHub 仓库](https://github.com/GuanYixuan/pyJianYingDraft)
- [剪映版本兼容性问题讨论](https://github.com/GuanYixuan/pyJianYingDraft/issues/115)
- [剪映 5.9 下载资源](https://github.com/GuanYixuan/pyJianYingDraft/discussions)

## 🎯 总结

**pyJianYingDraft 在 macOS 上完全可以使用**，但需要剪映版本匹配。

当前你的选择：
1. ✅ **继续使用** - 生成草稿，但需要在剪映 5.9 中打开
2. ⚠️ **降级剪映** - 获得完整兼容性
3. 🔮 **等待更新** - 项目可能支持新版本剪映

**推荐行动**：先验证草稿生成功能正常，再考虑是否需要降级剪映版本。